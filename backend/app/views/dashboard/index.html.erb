<div class="min-h-full">
  <nav class="bg-indigo-600">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="flex h-16 items-center justify-between">
        <div class="flex items-center">
          <div class="text-white text-xl font-bold">
            Realestate Image Explorer
          </div>
        </div>
        <div class="flex items-center">
          <div class="ml-4 flex items-center md:ml-6">
            <span class="text-white mr-4"><%= current_user.username %></span>
            <%= button_to "ログアウト", logout_path, method: :delete, class: "rounded-md bg-indigo-500 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-400" %>
          </div>
        </div>
      </div>
    </div>
  </nav>

  <main>
    <div class="mx-auto max-w-7xl py-6 sm:px-6 lg:px-8">
      <div class="px-4 py-6 sm:px-0">
        <h2 class="text-2xl font-semibold text-gray-900 mb-6">Welcome, <%= current_user.username %></h2>

        <% if current_user.google_token.present? %>
          <!-- Googleドライブのフォルダ一覧 -->
          <div class="bg-white shadow sm:rounded-lg">
            <div class="px-4 py-5 sm:p-6">
              <div class="flex justify-between items-start">
                <div>
                  <h3 class="text-lg font-medium leading-6 text-gray-900">Googleドライブのフォルダ</h3>
                  <div class="mt-2 max-w-xl text-sm text-gray-500">
                    <p>作業フォルダを選択してください。</p>
                  </div>
                </div>
                <%= button_to '連携を解除',
                    disconnect_google_path,
                    method: :delete,
                    data: { confirm: 'Google連携を解除してもよろしいですか？\n選択したフォルダの設定も削除されます。' },
                    class: 'text-sm text-red-600 hover:text-red-800 underline' %>
              </div>
              <div data-controller="folder-list" id="folder-list-container">
                <div data-folder-list-target="list" class="mt-5 bg-gray-50 rounded-md p-4 max-h-96 overflow-y-auto">
                  <!-- フォルダ一覧のローディング表示 -->
                  <div class="animate-pulse" data-folder-list-target="loading">
                    <div class="h-4 bg-gray-200 rounded w-3/4 mb-2"></div>
                    <div class="h-4 bg-gray-200 rounded w-1/2 mb-2"></div>
                    <div class="h-4 bg-gray-200 rounded w-2/3"></div>
                  </div>
                </div>
                <div class="mt-4 flex items-center justify-between">
                  <p class="text-sm text-gray-500" data-folder-list-target="status">
                    選択されたフォルダ: なし
                  </p>
                  <button type="button" id="select-folder" disabled
                    data-action="folder-list#useFolderClick"
                    class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 disabled:opacity-50 disabled:cursor-not-allowed">
                    このフォルダを使用
                  </button>
                </div>
              </div>
            </div>
          </div>
        <% else %>
          <!-- Google Drive連携カード -->
          <div class="bg-white overflow-hidden shadow rounded-lg">
            <div class="p-6">
              <div class="flex items-center">
                <div class="flex-shrink-0">
                  <svg class="h-8 w-8 text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v1m6 11h2m-6 0h-2m0 0H8m13 0a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                </div>
                <div class="ml-4">
                  <h3 class="text-lg font-medium text-gray-900">Googleドライブと連携</h3>
                  <p class="mt-1 text-sm text-gray-500">
                    Googleドライブと連携して、画像ファイルを簡単に管理できます。
                  </p>
                </div>
              </div>
              <div class="mt-6">
                <%= link_to 'Google連携を開始',
                    auth_google_oauth2_path,
                    class: 'inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500' %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </main>
</div>